<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>DuckDuckGo Stories: {{category}}</title>
    <link rel="stylesheet" type="text/css" href="style.css">
</head>
<body>
    <div class="header">
        <h1>DuckDuckGo Stories: {{category}}</h1>
        <p>
        {{#categories}}
        	<a href="/{{name}}">{{name}}({{amnt}})</a>
        {{/categories}}
        </p>
    </div>
    <div class="stories">

        {{#stories}}
        <div class="story">
            <div class="image">
                <img src="{{image}}" alt="image">
            </div>
            <div class="text">
                <a href="{{url}}">
                    <h3>{{title}}</h3>
                    <p>{{description}}</p>
                </a>
                <p>Category: {{category}}</p>
            </div>
        </div>
        {{/stories}}

    </div>
</body>
</html>
